# NPM Configuration
# SECURITY: Put private scopes/tokens in ~/.npmrc.local (untracked).

registry=https://registry.npmjs.org/
prefer-offline=true
save-exact=true

# Optional machine-local overrides (scopes/tokens) live here.
globalconfig=${HOME}/.npmrc.local
